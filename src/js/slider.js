jQuery(document).ready(function($){
    $('.slides.slick-slider-container').slick({
        infinite: true,
        dots: false,
        arrows: false,
        autoplay: true,
        autoplaySpeed: 5000
    });
});